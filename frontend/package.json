{
  "name": "ha_ci_demo-panel",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "esbuild src/ha-ci-demo-panel.ts --bundle --minify --format=esm --target=es2021 --outfile=../custom_components/ha_ci_demo/panel/ha-ci-demo-panel.js",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "lit": "^3.2.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
